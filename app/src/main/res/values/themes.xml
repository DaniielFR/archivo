<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Tema del sistema: solo sirve para la pantalla de arranque y el fondo de la
      ventana antes del primer frame de Compose. Todo lo demás lo gobierna
      ArchivoTheme en Kotlin.
    -->
    <style name="Theme.Archivo" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Archivo.Main</item>
    </style>

    <style name="Theme.Archivo.Main" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/system_neutral1_50</item>
    </style>
</resources>
